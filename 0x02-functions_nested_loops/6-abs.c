#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 *@n: the int to check
 *Returns: absolute value of integer
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = -n;
	}

	return (n);

}
