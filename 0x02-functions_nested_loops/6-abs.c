#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 *@n: the int to check
 *Returns: the absolute value of int
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = -n;
	}

	return (n);

}
